import { HeadingProps } from '@/types'

const Heading = ({
    title,
    description,
    icon,
    iconColor,
    bgColor,
}: HeadingProps) => {
    return <div>Heading</div>
}

export default Heading
